name: HERE Core UI Platform Starter - Unit Tests

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    paths:
      - 'how-to/workspace-platform-starter/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Node 24
        uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          scope: '@openfin'

      - name: Restore root node_modules from cache
        id: node-cache
        uses: actions/cache@v6
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm ci

      - name: Run HERE Core UI Platform Starter Unit Tests
        working-directory: ./how-to/workspace-platform-starter
        run: npm run test
