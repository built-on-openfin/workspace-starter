# Fixture Platform

A very simple HERE Core Workspace platform for demonstrating automation testing capabilities.

## Getting Started

To run the platform, execute the following command in the terminal:

```bash
npm install
npm start
```

This will start a Vite dev server hosting the manifest.

### Launching the platform

```
npm run launch
```

Or open fin://localhost:8080/manifest.fin.json in your browser