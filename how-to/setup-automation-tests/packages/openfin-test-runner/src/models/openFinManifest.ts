/**
 * Definition of the OpenFin manifest.
 */
export interface OpenFinManifest {
    /**
     * The runtime details.
     */
    runtime: {
        /**
         * The version of the runtime.
         */
        version: string;
    };
}
