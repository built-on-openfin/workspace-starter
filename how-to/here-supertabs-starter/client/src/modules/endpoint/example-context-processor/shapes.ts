/**
 * Settings for the context process.
 */
export interface ContextProcessorSettings {
	/**
	 * Should we log the context during processing.
	 */
	logContext: boolean;

	/**
	 * Should we log the processed context.
	 */
	logProcessedContext: boolean;
}
