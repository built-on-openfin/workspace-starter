/**
 * Options for the example content creation provider.
 */
export interface ExampleContentCreationProviderOptions {
	/**
	 * Example property that is configured in the manifest.
	 */
	exampleProp?: string;
}
