/**
 * Options for the example integrations provider.
 */
export interface ExampleIntegrationsProviderOptions {
	/**
	 * Example property that is configured in the manifest.
	 */
	exampleProp?: string;
}
