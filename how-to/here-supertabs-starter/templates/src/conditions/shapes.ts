/**
 * Options for the example conditions provider.
 */
export interface ExampleConditionsProviderOptions {
	/**
	 * Example property that is configured in the manifest.
	 */
	exampleProp?: string;
}
